<?xml version='1.0' encoding='utf-8' standalone='yes' ?>
<!-- This is a place for vendor-specific default config values. The format and
     filtering rules are the same as those in carrier_config*.xml files. This
     file is read before any specific config file from the assets folder. -->
<carrier_config_list />
